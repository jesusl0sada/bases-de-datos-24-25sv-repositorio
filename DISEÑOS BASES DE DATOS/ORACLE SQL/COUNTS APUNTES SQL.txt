COUNT ORACLE SQL --> SELECT COUNT()
SI LE QUEREMOS METER UN ALIAS A LA TABLA PARA QUE NO SE LLAME COUNT SERÍA --> COUNT() AS (texto)  // AS = ALIAS. 

SELECT COUNT(*) 
FROM EMPLE
WHERE oficio = 'VENDEDOR';


------- CON ALIAS --------

SELECT COUNT(*) AS VENDEDORES
FROM EMPLE
WHERE oficio = 'VENDEDOR';